import type { Logger } from '../../../foundation/logger/create-logger';
import type { MediaContext } from '../context';
export declare function useMediaEventsLogger({ $player, $store }: MediaContext, logger?: Logger): void;
